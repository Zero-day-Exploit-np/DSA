
    // printf("element is dequeue = %d\n", dequeue(a));
    // printf("element is dequeue = %d\n", dequeue(a));
    // printf("element is dequeue = %d\n", dequeue(a));
    // printf("element is dequeue = %d\n", dequeue(a));
    // printf("element is dequeue = %d\n", dequeue(a));
    // printf("element is dequeue = %d\n", dequeue(a));
    // printf("element is dequeue = %d\n", dequeue(a));
    // printf("element is dequeue = %d\n", dequeue(a));
    // printf("element is dequeue = %d\n", dequeue(a));
    // printf("element is dequeue = %d\n", dequeue(a));